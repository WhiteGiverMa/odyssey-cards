# 待确认问题

这些问题来自 GDD、draft、handoff、资源和代码之间的新旧冲突。本轮不擅自裁决。

## 命名冲突

1. `闪击` vs `冲锋`
   - 旧 GDD 写“冲锋”。
   - 当前代码注释和 README 写“闪击”。
   - 建议确认最终中文名。

2. `棍木` vs `滚木`
   - 旧草稿出现“滚木”。
   - 当前代码和本地化 key 指向“棍木”。
   - 建议确认是否统一为“棍木”。

3. 英文全称
   - 草稿曾写 `Shoujou Odyssey Cards`，README 当前是 `Shoujo Odyssey Cards`。
   - `Shoujo` 更常见；如有品牌设定需确认。

## 规则冲突

1. 宇宙员 E 意图热力阈值
   - 草稿：热力达到 200% 时，下一个回合固定 E。
   - 当前代码：`CurrentHeat >= 1.2f`，即 120%。
   - 需要确认最终阈值。

2. 「解释」卡牌类型
   - 设计语义：状态牌/敌人塞入的干扰牌。
   - 当前资源：Type=Spell，Rarity=1，并带奇巧。
   - 需要确认是否改为 Status / StatusToken。

3. 「轮战」回到抽牌堆位置
   - 旧设计：随机位置。
   - 新草稿：抽牌堆底部。
   - 当前代码注释：底部。
   - 建议确认底部为最终设计。

4. 敌方默认武器是否影响意图伤害
   - 早期草稿曾写“不影响意图造成的伤害”。
   - 5/29 修改与当前代码语义：敌方攻击力会附加到意图伤害。
   - 建议视为新设计，但可确认。

## 完成度待 UX 验证

1. 英雄技能“铁腕”是否已作为正式玩家入口接线。
2. 磁轨手枪是否已在宇宙员战斗中完整表现。
3. 飞远领域的最终语义。
4. 奖励、商店、休息处、事件房间是否按当前 UX 视为“存在”。
5. 移动端 Day1 规则哪些已完成，哪些仍只是审计建议。
6. 四夜雷电光/星途精神改造为限时挂载（走 StatusEffect 通道）后，需在运行时 QA 中确认 4 回合伤害触发与下回合额外抽2+2法力触发仍按预期生效，以及效果图标是否仍正常显示（`EffectBar` 读 `StatusEffect` 路径已是默认行为）。
7. 火力筛选（理惠技能）改读 `CardMechanicTag.DirectDamage` 后，所有直伤 .tres 的 tag 标注完整性需要审计；漏打 tag 的卡会让此技能抽牌逻辑提前停止。用户已自行接手审计。
